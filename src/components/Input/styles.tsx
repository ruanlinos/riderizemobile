import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inputContainer: {
    flexDirection: 'column',
    flex: 1,
    marginBottom: 10,
  },
  labelText: {
    textAlign: 'left',
    color: '#BCBCBC',
    fontSize: 12,
    marginBottom: 4,
  },
});
